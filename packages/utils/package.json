{
  "name": "@example/utils",
  "version": "0.0.1",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "BABEL_ENV=build babel src --root-mode upward --out-dir dist --extensions .ts,.tsx --delete-dir-on-start --no-comments",
    "build:declaration": "tsc --project tsconfig.build.json"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
